/** Initializes a new Mario Demo game and starts running it. */
class Main {
	function void main() {
		var MarioGame newGame;
		let newGame = MarioGame.new();
		do newGame.run();
		do newGame.dispose();
		return;
	}
}